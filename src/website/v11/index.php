<?php
declare(strict_types=1);
final class Vault {
    private static ?PDO $db = null;
    static function db(): PDO { return self::$db ??= new PDO('sqlite:' . __DIR__ . '/../v9/vault.sqlite', null, null, [PDO::ATTR_ERRMODE=>PDO::ERRMODE_EXCEPTION, PDO::ATTR_DEFAULT_FETCH_MODE=>PDO::FETCH_ASSOC]); }
    static function json(string $path): ?array { $s=self::db()->prepare('SELECT data FROM vault WHERE path=?');$s->execute([$path]);$r=$s->fetch();return $r?json_decode((string)$r['data'],true):null; }
    static function image(string $path): never { $s=self::db()->prepare('SELECT mime,data FROM vault WHERE path=?');$s->execute([$path]);$r=$s->fetch();if(!$r){http_response_code(404);exit('Not found');}header('Content-Type: '.$r['mime']);header('Cache-Control: public,max-age=31536000,immutable');echo $r['data'];exit; }
}
function base_path(): string { if($base=$_SERVER['TAROT_LOCAL_BASE_PATH']??'')return $base;$dir=str_replace('\\','/',dirname($_SERVER['SCRIPT_NAME']??'/'));return $dir==='/'||$dir==='.'?'':$dir; }
$base=base_path();$path=urldecode(parse_url($_SERVER['REQUEST_URI']??'/',PHP_URL_PATH)?:'/');if($base!==''&&str_starts_with($path,$base))$path=substr($path,strlen($base));$path='/'.trim($path,'/');if(isset($_GET['img']))Vault::image('/img/'.urldecode((string)$_GET['img']));if($path!=='/'){http_response_code(404);exit('404');}
$data=Vault::json('/app-data.json');if(!$data){http_response_code(500);exit('Vault incomplet');}
$lames=[];foreach($data['cards'] as $c)$lames[]=['id'=>$c['id'],'n'=>$c['name'],'f'=>$c['fam'],'num'=>$c['num'],'up'=>$c['keywords_up']??'','dn'=>$c['keywords_down']??'','rep'=>trim((string)($data['es'][$c['id']]['rep']??''))];
$fams=[];foreach($data['families'] as $f)$fams[$f['key']]=['n'=>$f['name'],'short'=>$f['short']??$f['name'],'sym'=>$f['sym']??'✦','ac'=>$f['ac']??'#d8b24a'];
$lj=json_encode($lames,JSON_UNESCAPED_UNICODE|JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|JSON_HEX_QUOT);$fj=json_encode($fams,JSON_UNESCAPED_UNICODE|JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|JSON_HEX_QUOT);$ver='2026.09.02';
?><!doctype html><html lang="fr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Arcana Index — Tarot v<?= $ver ?></title><style>
:root{--bg:#07070a;--fg:#eee9df;--muted:#77727c;--gold:#d8b24a;--line:#2c2934;--glass:rgba(10,9,14,.86)}*{box-sizing:border-box}html,body{margin:0;min-height:100%;background:var(--bg);color:var(--fg);font-family:ui-monospace,SFMono-Regular,Menlo,monospace}body{overflow:hidden}#stage{position:fixed;inset:0;cursor:grab}#stage.drag{cursor:grabbing}#stage canvas{display:block}.hud{position:fixed;z-index:4;pointer-events:none}.hud>*{pointer-events:auto}#brand{top:22px;left:28px;letter-spacing:.28em;font-size:13px}#brand b{color:var(--gold);font-weight:400}#brand small{display:block;color:var(--muted);font-size:10px;letter-spacing:.16em;margin-top:8px}.tools{top:18px;right:24px;display:flex;gap:8px}.tools a,.tools button{background:var(--glass);border:1px solid var(--line);color:var(--muted);padding:10px 13px;border-radius:3px;text-decoration:none;font:inherit;font-size:10px;letter-spacing:.14em;cursor:pointer}.tools a:hover,.tools button:hover{color:var(--fg);border-color:var(--gold)}#searchDock{top:78px;left:50%;transform:translateX(-50%);width:min(620px,calc(100vw - 32px));display:flex;align-items:center;gap:12px}.search{width:100%;background:var(--glass);border:1px solid var(--line);color:var(--fg);padding:14px 17px;outline:0;font:inherit;font-size:12px;letter-spacing:.08em}.search:focus{border-color:var(--gold);box-shadow:0 0 0 3px rgba(216,178,74,.1)}#count{color:var(--gold);font-size:10px;white-space:nowrap}.filters{left:28px;bottom:24px;display:flex;gap:6px;max-width:calc(100vw - 56px);overflow:auto}.filter{background:var(--glass);border:1px solid var(--line);color:var(--muted);padding:8px 10px;border-radius:3px;font:inherit;font-size:9px;letter-spacing:.08em;white-space:nowrap}.filter.on,.filter:hover{border-color:var(--gold);color:var(--fg)}.filter i{color:var(--gold);font-style:normal;margin-right:5px}#hint{right:26px;bottom:28px;color:var(--muted);font-size:9px;letter-spacing:.16em}#drawer{position:fixed;z-index:10;inset:0;background:rgba(0,0,0,.4);opacity:0;pointer-events:none;transition:opacity .3s}#drawer.open{opacity:1;pointer-events:auto}.sheet{position:absolute;right:0;top:0;bottom:0;width:min(430px,92vw);overflow:auto;background:var(--glass);backdrop-filter:blur(18px);border-left:1px solid var(--gold);padding:58px 30px 30px}.close{position:absolute;top:18px;right:18px;width:34px;height:34px;border:1px solid var(--line);border-radius:50%;background:none;color:var(--muted);font-size:18px}.close:hover{border-color:var(--gold);color:var(--gold)}.eyebrow{color:var(--gold);font-size:10px;letter-spacing:.18em}.sheet h1{font:italic 400 48px Georgia,serif;letter-spacing:-.04em;margin:11px 0 22px}.portrait{display:flex;gap:18px}.portrait img{width:135px;height:203px;object-fit:cover;border:1px solid var(--gold);box-shadow:0 0 30px rgba(216,178,74,.2)}.pill{display:inline-block;border:1px solid var(--gold);border-radius:20px;color:var(--gold);padding:7px 9px;font-size:10px;margin:0 5px 10px 0}.answer{color:#a9d8aa;border-color:#55835d}.label{color:var(--muted);font-size:9px;letter-spacing:.18em;margin:20px 0 8px}.keywords{display:flex;flex-wrap:wrap;gap:5px}.keywords span{border:1px solid var(--line);padding:6px 7px;color:#c4bdaf;font-size:9px}.keywords.down span{color:#e7a8ad;border-color:#63343d}.v9{display:inline-block;margin-top:26px;border:1px solid var(--line);color:var(--muted);padding:10px 12px;text-decoration:none;font-size:9px;letter-spacing:.12em}.v9:hover{color:var(--gold);border-color:var(--gold)}
@media(max-width:640px){#brand{top:15px;left:16px;font-size:11px}#brand small{font-size:9px}.tools{top:14px;right:14px}.tools a,.tools button{padding:8px 9px;font-size:9px}.tools a{display:none}#searchDock{top:100px}.filters{left:14px;bottom:15px;max-width:calc(100vw - 28px)}#hint{display:none}.sheet{top:auto;left:0;right:0;width:100%;height:65dvh;border-left:0;border-top:1px solid var(--gold);border-radius:16px 16px 0 0;padding:45px 20px 25px}.sheet h1{font-size:39px}.portrait img{width:112px;height:168px}}
</style></head><body><div id="stage"></div><div class="hud" id="brand">ARCANA <b>INDEX</b><small>78 LAMES · V<?= $ver ?></small></div><div class="hud tools"><button id="shuffle">Mélanger</button><a href="../v9/">V9</a></div><div class="hud" id="searchDock"><input class="search" id="search" type="search" placeholder="Chercher une lame ou un numéro…" autocomplete="off"><span id="count"></span></div><nav class="hud filters" id="filters"></nav><div class="hud" id="hint">GLISSER · MOLETTE · CLIQUER UNE LAME</div><div id="drawer"><section class="sheet"><button class="close" id="close">×</button><div class="eyebrow" id="dFam"></div><h1 id="dName"></h1><div class="portrait"><img id="dImg" alt=""><div><span class="pill" id="dKey"></span><span class="pill answer" id="dRep"></span><div class="label">ENDROIT</div><div class="keywords" id="dUp"></div><div class="label">ENVERS</div><div class="keywords down" id="dDn"></div></div></div><a class="v9" id="dLink" target="_blank" rel="noopener">FICHE COMPLÈTE · V9 ↗</a></section></div>
<script type="importmap">{"imports":{"three":"https://cdn.jsdelivr.net/npm/three@0.170.0/build/three.module.js"}}</script><script type="module">
import * as THREE from 'three';
const LAMES=<?= $lj ?>,FAMS=<?= $fj ?>,B=<?= json_encode($base,JSON_UNESCAPED_SLASHES) ?>,stage=document.getElementById('stage'),scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(38,innerWidth/innerHeight,.1,100);camera.position.set(0,0,28);camera.lookAt(0,0,0);
const renderer=new THREE.WebGLRenderer({antialias:true,alpha:false});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(innerWidth,innerHeight);renderer.setClearColor(0x08070b,1);stage.appendChild(renderer.domElement);
const rack=new THREE.Group(),textureLoader=new THREE.TextureLoader(),cards=[],ray=new THREE.Raycaster(),pointer=new THREE.Vector2();rack.position.y=-.7;scene.add(rack);let hovered=null,selected=null,drag=null,filter='',activeFam='';
const familyKeys=Object.keys(FAMS),byFamily=Object.fromEntries(familyKeys.map(k=>[k,[]]));LAMES.forEach(l=>(byFamily[l.f]??=[]).push(l));
const cardGeo=new THREE.PlaneGeometry(.92,1.38),COLS=11;
function cardMesh(l){const list=byFamily[l.f],index=list.indexOf(l),row=familyKeys.indexOf(l.f)*2+Math.floor(index/COLS),col=index%COLS,x=(col-(COLS-1)/2)*1.08,y=(4.5-row)*1.48,z=(row%2)*.08;const tex=textureLoader.load(B+'/index.php?img='+encodeURIComponent(l.id+'.jpg'));tex.colorSpace=THREE.SRGBColorSpace;const mat=new THREE.MeshBasicMaterial({map:tex});const m=new THREE.Mesh(cardGeo,mat);m.position.set(x,y,z);m.rotation.z=(Math.random()-.5)*.025;m.userData={l,home:m.position.clone(),homeR:m.rotation.z};rack.add(m);cards.push(m)}
LAMES.forEach(cardMesh);
const starPos=[];for(let i=0;i<700;i++)starPos.push((Math.random()-.5)*30,(Math.random()-.5)*18,-2-Math.random()*4);const starGeo=new THREE.BufferGeometry();starGeo.setAttribute('position',new THREE.Float32BufferAttribute(starPos,3));scene.add(new THREE.Points(starGeo,new THREE.PointsMaterial({color:0xd8b24a,size:.035,transparent:true,opacity:.7})));
const halo=new THREE.Mesh(new THREE.TorusGeometry(9,.012,4,180),new THREE.MeshBasicMaterial({color:0x7353a5,transparent:true,opacity:.3}));halo.position.z=-1;scene.add(halo);
function words(s){return String(s||'').split(',').map(x=>x.trim()).filter(Boolean)}function img(l){return B+'/index.php?img='+encodeURIComponent(l.id+'.jpg')}function visible(){const q=filter.toLocaleLowerCase();return LAMES.filter(l=>(!activeFam||l.f===activeFam)&&(!q||l.n.toLocaleLowerCase().includes(q)||String(l.num).padStart(2,'0').includes(q)))}
function renderFilters(){document.getElementById('filters').innerHTML='<button class="filter '+(!activeFam?'on':'')+'" data-fam="">✦ TOUTES</button>'+familyKeys.map(k=>'<button class="filter '+(activeFam===k?'on':'')+'" data-fam="'+k+'"><i>'+FAMS[k].sym+'</i>'+FAMS[k].short+'</button>').join('')}
function applyFilter(){const set=new Set(visible().map(l=>l.id));cards.forEach(m=>{const yes=set.has(m.userData.l.id);m.visible=yes||(!filter&&!activeFam);m.scale.setScalar(yes?1:.01)});document.getElementById('count').textContent=visible().length+' / '+LAMES.length}
function openCard(l){if(!l)return;selected=l;document.getElementById('dFam').textContent=String(l.num).padStart(2,'0')+' · '+(FAMS[l.f]?.n||'');document.getElementById('dName').textContent=l.n;document.getElementById('dImg').src=img(l);document.getElementById('dImg').alt=l.n;document.getElementById('dKey').textContent=words(l.up)[0]||'ARCHIVE';const rep=document.getElementById('dRep');rep.textContent=l.rep;rep.style.display=l.rep?'':'none';document.getElementById('dUp').innerHTML=words(l.up).map(x=>'<span>'+x+'</span>').join('');document.getElementById('dDn').innerHTML=words(l.dn).map(x=>'<span>'+x+'</span>').join('');document.getElementById('dLink').href='../v9/?carte='+encodeURIComponent(l.id);document.getElementById('drawer').classList.add('open');history.replaceState(null,'',location.pathname+'?carte='+encodeURIComponent(l.id));const m=cards.find(x=>x.userData.l.id===l.id);if(m){m.userData.focus=true;m.userData.from=m.position.clone();}}
function closeCard(){document.getElementById('drawer').classList.remove('open');if(selected){const m=cards.find(x=>x.userData.l.id===selected.id);if(m)m.userData.focus=false}selected=null;history.replaceState(null,'',location.pathname)}
document.getElementById('search').oninput=e=>{filter=e.target.value.trim();applyFilter()};document.getElementById('filters').onclick=e=>{const b=e.target.closest('[data-fam]');if(b){activeFam=b.dataset.fam;renderFilters();applyFilter()}};document.getElementById('close').onclick=closeCard;document.getElementById('drawer').onclick=e=>{if(e.target.id==='drawer')closeCard()};document.getElementById('shuffle').onclick=()=>{cards.forEach(m=>{m.userData.home.x+=(Math.random()-.5)*.24;m.userData.home.y+=(Math.random()-.5)*.24});};document.onkeydown=e=>{if(e.key==='Escape')closeCard();if(e.key==='/'&&!/INPUT/i.test(e.target.tagName)){e.preventDefault();document.getElementById('search').focus()}};
stage.onpointerdown=e=>{drag={x:e.clientX,y:e.clientY,rx:rack.rotation.x,ry:rack.rotation.y,moved:false};stage.classList.add('drag')};stage.onpointermove=e=>{if(drag){const dx=e.clientX-drag.x,dy=e.clientY-drag.y;if(Math.abs(dx)+Math.abs(dy)>5)drag.moved=true;rack.rotation.y=drag.ry+dx*.002;rack.rotation.x=THREE.MathUtils.clamp(drag.rx-dy*.0015,-.28,.28)}else{pointer.x=e.clientX/innerWidth*2-1;pointer.y=-(e.clientY/innerHeight)*2+1;ray.setFromCamera(pointer,camera);const hit=ray.intersectObjects(cards.filter(m=>m.visible))[0];hovered=hit?.object||null;stage.style.cursor=hovered?'pointer':'grab'}};stage.onpointerup=e=>{if(!drag)return;stage.classList.remove('drag');if(!drag.moved&&hovered)openCard(hovered.userData.l);drag=null};stage.onwheel=e=>{camera.position.z=THREE.MathUtils.clamp(camera.position.z+e.deltaY*.01,19,32)};
function tick(t){requestAnimationFrame(tick);cards.forEach((m,i)=>{const u=m.userData;if(u.focus){m.position.lerp(new THREE.Vector3(0,0,10),.1);m.rotation.set(0,0,0);m.scale.setScalar(1.65)}else{m.position.lerp(u.home,.08);m.rotation.z+=(u.homeR-m.rotation.z)*.08;const s=m===hovered?1.12:1;m.scale.lerp(new THREE.Vector3(s,s,s),.12);m.position.y+=(u.home.y+Math.sin(t*.001+i)*.025-m.position.y)*.08}});rack.rotation.y+=.0001;halo.rotation.z=t*.00002;renderer.render(scene,camera)}
renderFilters();applyFilter();const initial=new URLSearchParams(location.search).get('carte');if(initial){const l=LAMES.find(x=>x.id===initial);if(l)setTimeout(()=>openCard(l),500)}addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});tick(0);
</script></body></html>
